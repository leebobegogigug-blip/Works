# {{NAME}} 매뉴얼

{{TAGLINE}}. 소개는 [README](../README.md), 설치 절차는 [INSTALL.md](../INSTALL.md)에 있습니다.

- **(특징)** — (한 줄)
- **내 PC 전용** — `127.0.0.1`에만 열리고, 밖으로 나가는 통신은 (설정한 주소)뿐

## 설정

| 키 | 설명 |
|---|---|
| `port` | 기본 (대장에 등록한 대역의 첫 번호) |

## 쓰는 법

- (가장 흔한 사용 한 줄)

## 화면

<!-- 번호 붙은 구역 (docs/DESIGN.md › 원칙 3) -->
| # | 구역 | 보여 주는 것 |
|---|---|---|
| 01 | **(NAME)** | (무엇) |

## 디자인

works 공통 규격은 [docs/DESIGN.md](../../docs/DESIGN.md) 에 있습니다. 이 앱에서는 이렇게 보입니다.

<!-- 원칙 번호와 이름은 DESIGN.md 그대로. 색 값 · 팔레트 표는 옮겨 적지 않는다 (RULES.md › W-10) -->
| # | 원칙 | {{NAME}} 에서 |
|---|---|---|
| 1 | **한 화면 = 한 모드** | |
| 2 | **색 = 조작** | |
| 3 | **번호 붙은 구역** | |
| 4 | **엔지니어링을 숨기지 않기** | |
| 5 | **즉각 반응** | |
| 6 | **사각 격자** | |
| 7 | **캐릭터** | |

## 개발

| 할 일 | 명령 |
|---|---|
| 테스트 | `python -m unittest discover -s tests` (표준 라이브러리만) |
| works 규칙 검사 | 저장소 루트에서 `python tools/works_check.py` |

CI(`.github/workflows/{{APP}}.yml`)는 Windows · Ubuntu × Python 3.8 · 최신에서 테스트를 돌립니다.

## 데이터

`%LOCALAPPDATA%\{{APP}}\` 에 저장합니다. 저장소에는 들어가지 않습니다.

| 파일 | 내용 |
|---|---|
| (파일) | (무엇) |

## 알려진 한계

- (규약을 따르지 않는 곳이 있으면 이유와 함께 여기에 — RULES.md › 등급)

## 라이선스

(내장한 폰트 · 자산이 있으면 출처와 라이선스 파일 경로)
