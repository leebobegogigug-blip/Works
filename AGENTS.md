# works — 에이전트 규칙

> **적용 범위.** 이 파일은 works 저장소의 코드 · 문서를 **고치는** 작업에만 적용한다.
> 앱 설치(`INSTALL.md` 따라 하기) · 앱 사용 · 이 폴더 아래의 다른 프로젝트 작업 중이라면 이 파일을 무시하고 원래 지시를 따른다.

## 시작하기 전에

1. [RULES.md](RULES.md) 를 끝까지 읽는다. 화면 · 문서 · 이미지를 만지면 [docs/DESIGN.md](docs/DESIGN.md) 도 읽는다.
2. 새 앱이면 코드보다 먼저 [docs/REGISTRY.md](docs/REGISTRY.md) 에 등록한다 (RULES.md › 새 앱 만드는 순서).
3. 규칙과 부딪히는 요청을 받으면, 하기 전에 어느 조항과 부딪히는지 사용자에게 말한다.

## 헌법 요약 — 정본은 RULES.md

- W-01 **폴더 = 앱 = 명령** — 앱은 `<name>-<n>/` 하나에 완결. 다른 앱과는 대장에 올린 공개 명령으로만
- W-02 **의존성 0** — 실행은 Python 3.8+ 표준 라이브러리만
- W-03 **내 PC 에서만** — `127.0.0.1` 바인딩, 밖으로는 사용자가 설정한 주소만
- W-04 **비밀은 어디에도 찍지 않는다** — 출력 · 로그 · 명령줄 · 커밋 모두
- W-05 **사용자 원문은 메모리에만** — 확정한 것만 저장
- W-06 **저장은 깨지지 않게** — 버전 · 원자적 쓰기 · `%LOCALAPPDATA%\<앱>`
- W-07 **바꾸기 전에 보여 준다** — 제안 → 확정, 되돌리기
- W-08 **설치는 에이전트가, 되돌릴 수 있게** — INSTALL.md 필수 절 · 관리자 권한 금지
- W-09 **이름을 바꾸면 데려간다** — 자동 이전 · 덮어쓰지 않기
- W-10 **디자인은 하나** — docs/DESIGN.md · 네이비 · 라임 · 회색 · 빨강 없음
- W-11 **약속은 CI 가 지킨다** — 최소 Python 포함 · 가짜 서버 · 검증 가능한 숫자만
- W-12 **공개 저장소다** — 사내 정보 금지 · 영감 고지문 · 라이선스

## 끝내기 전에

- 고친 앱의 `docs/MANUAL.md` › 개발 절에 적힌 검사와 테스트를 돌린다.
- `python tools/works_check.py` — 새 경고가 없어야 한다. 예외 대장의 줄을 고쳤으면 그 줄을 지운다.
- 커밋: 한국어 `<name>-<n>: 무엇 · 무엇` (저장소 전체는 `works: …`). 한 커밋에 앱 하나.
- 사내 LLM 을 쓰는 앱은 docs/SPEC-llm.md 의 설정 키 · 명령을 따른다 (S-09).
- 키 · 토큰은 출력하지 않는다.
