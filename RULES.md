# works 규칙

works 저장소의 모든 앱이 따르는 규칙의 **정본**이다. 규칙은 여기에만 적는다.
다른 문서는 이 파일을 링크하고, 내용을 옮겨 적지 않는다 (옮겨 적은 규칙은 반드시 갈라진다).

- 디자인 규격 → [docs/DESIGN.md](docs/DESIGN.md)
- 앱 대장 · 예외 대장 → [docs/REGISTRY.md](docs/REGISTRY.md)
- 문서 뼈대 → [docs/templates/](docs/templates/)
- 에이전트용 요약 → [AGENTS.md](AGENTS.md) (opencode) · [CLAUDE.md](CLAUDE.md) (Claude Code)

## 등급

| 등급 | 뜻 | 어기면 |
|---|---|---|
| **헌법 W** | 모든 앱이 지킨다 | 머지하지 않는다. 바꾸려면 이 파일을 고치는 PR 이 먼저 들어간다 |
| **규약 S** | 따른다 | 따르지 않을 때는 이유를 그 앱 MANUAL 의 `알려진 한계` 나 PR 에 적는다 |

이미 있는 앱이 아직 못 지키는 조항은 [예외 대장](docs/REGISTRY.md#예외-대장)에 기한과 함께 적는다. **대장에 없는 위반은 위반이다.**

---

## 헌법

### W-01 폴더 = 앱 = 명령
- 앱은 저장소 루트의 `<name>-<n>/` 폴더 하나에 완결된다. 폴더 이름이 곧 명령 이름이다.
- 다른 앱 폴더의 코드를 import · 실행 · 경로로 참조하지 않는다. 앱끼리는 **규격**(docs/DESIGN.md)만 공유하고 **코드**는 공유하지 않는다.
- 필수 구성: `README.md` · `INSTALL.md` · `docs/MANUAL.md` · `tests/` · `.gitignore` · `.gitattributes` · `.github/workflows/<name>-<n>.yml`

왜: 한 앱을 고쳐도 다른 앱이 깨지지 않게. 폴더를 지우면 제거가 끝나게.

### W-02 의존성 0
- 실행에는 Python 3.8+ 표준 라이브러리만 쓴다. `pip install` 없이 받아서 바로 돈다.
- 선택 기능의 외부 패키지(예: Outlook 연동의 `pywin32`)는 함수 안에서 import 하고, 없으면 그 기능만 꺼진다.
- 개발 도구(`tests/` · `tools/`)는 외부 패키지를 써도 되지만, 실행 경로에서 import 하지 않는다.

### W-03 내 PC 에서만
- 앱이 여는 서버는 `127.0.0.1` 에만 바인딩한다.
- 밖으로 나가는 통신은 사용자가 설정한 주소(사내 LLM · opencode 등)뿐이다. 텔레메트리 · 자동 업데이트 확인 · 외부 CDN · 웹 폰트 금지.
- 브라우저 UI 가 있으면 실행마다 새 토큰을 쓰고 `Host` 헤더를 검사한다.

### W-04 비밀은 어디에도 찍지 않는다
- 키 · 토큰 · 비밀번호를 화면 출력 · 로그 · 명령줄 인자 · 커밋에 남기지 않는다. 보여 줄 때는 `설정됨 (n자)` 처럼 가린다.
- 설정 파일은 값 대신 `{env:이름}` · `{file:경로}` 참조를 받는다 (OpenCode 와 같은 문법).
- 비밀 값을 명령줄로 받는 옵션을 만들지 않는다. 다른 프로세스에 넘길 때는 파일로 넘긴다.

### W-05 사용자 원문은 메모리에만
- 대화 · 입력 원문은 디스크에 남기지 않는다. 사용자가 확정한 것만 저장하고, 무엇이 저장되는지 화면에 미리 보인다.

### W-06 저장은 깨지지 않게
- 저장 파일에는 형식 버전을 적는다. 쓰기는 `.tmp` 에 쓴 뒤 바꿔치기(원자적)한다.
- 읽다가 깨져 있으면 `.broken` 으로 백업하고 새로 시작한다. 더 새 버전의 파일이면 덮어쓰지 않고 읽기 전용으로 연다.
- 사용자 데이터는 `%LOCALAPPDATA%\<name>-<n>\` (Windows 밖에서는 `~/.local/share/<name>-<n>/`) 에 둔다. 저장소 폴더 안에 두지 않는다.

왜: `git pull` · `git clean` · 폴더 교체가 사용자 데이터를 건드리지 않게.

### W-07 바꾸기 전에 보여 준다
- 앱 밖의 상태(캘린더 · 파일 · 다른 프로그램의 설정)를 바꾸는 동작은 **제안 → 확정** 2단계다. 확정 전에는 아무것도 바꾸지 않는다.
- 되돌릴 수 있는 동작이면 되돌리기를 준다.

### W-08 설치는 에이전트가, 되돌릴 수 있게
- `INSTALL.md` 는 opencode 가 그대로 따라 하는 절차다. 필수: 규칙 절 · 단계마다 확인과 실패했을 때 할 일 · `[질문]` 표시 · 보고 형식 · 문제 해결 · 업데이트 · 제거.
- 관리자 권한 금지. 사용자 범위 변경(사용자 PATH · HKCU · Windows Terminal 설정 · 시작 프로그램)은 `[질문]` 으로 동의를 받고, 백업하고, **제거 절에 되돌리는 방법이 있을 때만** 한다.
- 설치 위치는 `D:\OPENCODE\<name>-<n>` 이다 (저장소 = `D:\OPENCODE`).

### W-09 이름을 바꾸면 데려간다
- 이름은 [대장](docs/REGISTRY.md)에 등록할 때 정한다. 머지한 뒤에는 바꾸지 않는 것이 원칙이다.
- 바꿔야 하면: 예전 이름의 데이터 · 실행기 · 자동 실행 · PATH 를 첫 실행 때 자동으로 옮긴다. 덮어쓰지 않고, 둘 다 있으면 사람에게 맡기고, 예전 프로그램이 켜져 있으면 미룬다.
- 옮기는 코드를 언제 지울지 대장의 `예전 이름` 칸에 적는다.

### W-10 디자인은 하나
- 화면과 문서 이미지는 [docs/DESIGN.md](docs/DESIGN.md) 를 따른다. 유채색은 네이비 · 라임 두 계열만, 나머지는 무채색이다. **빨강 없음.**
- 앱 문서는 디자인 규칙 · 팔레트 값을 옮겨 적지 않고 DESIGN.md 를 링크한다. 앱마다 "이 앱에서는 어디에" 를 적는 것은 된다 (원칙 번호와 이름은 DESIGN.md 그대로).

### W-11 약속은 CI 가 지킨다
- 앱마다 워크플로 하나(`paths:` 로 자기 폴더만)를 둔다. Windows + Ubuntu × (README 에 약속한 최소 Python, 최신 Python).
- 테스트는 표준 라이브러리 `unittest`. 사내 LLM · opencode 같은 외부 서비스는 가짜 서버로 흉내 낸다.
- README 에 적는 숫자와 사양(최소 Python · 테스트 수 등)은 CI 나 검사기가 확인할 수 있는 것만 적는다.

### W-12 공개 저장소다
- 이 저장소는 공개다. 사내 주소 · 회사명 · 동료 실명 · 내부 시스템 이름 · 실제 일정이나 대화가 찍힌 스크린샷을 커밋하지 않는다. 예시는 지어낸 것만 쓴다 (주소는 `example.com` · `*.local` 처럼 실재하지 않는 것).
- 앱 README 끝에 영감 고지문을 둔다: "화면 문법은 소형 하드웨어 계측기(Teenage Engineering 류)에서 영감을 받았고, 해당 회사와는 관련이 없습니다."
- 내장한 폰트 · 자산은 라이선스 파일을 함께 둔다.

---

## 규약

| # | 규약 |
|---|---|
| S-01 | **이름.** 화면 표기는 `Name–N` (en dash `–`, U+2013), 폴더 · 명령 · 파일 · 데이터 폴더는 `name-n`. N 은 모델 번호라서 업데이트로 바뀌지 않는다. 앱 안의 부품은 `XX–N` (예: `TQ–1`). 코드 상수는 `APP` · `NAME` · `VERSION` |
| S-02 | **버전.** `VERSION = "x.y.z"` 상수 하나를 두고, `--version` (명령형 앱은 `version`) 으로 보여 준다 |
| S-03 | **명령과 종료 코드.** 공통 동작의 이름은 `setup` · `check` · `status` · `stop` · `version` · `help`. 종료 코드는 `0` OK · `1` 점검 실패 · `2` 사용법 오류 · `3` 사람 확인 필요. 점검 출력의 마지막 줄은 `결과: OK` · `결과: 확인 필요` · `결과: 점검 실패` 중 하나다 (에이전트가 이 줄로 판단한다) |
| S-04 | **앱 유형.** *웹형*(Secretary–1): 한 파일 배포, UI 는 `ui.html` 을 `build.py` 로 내장하고 CI 에서 `--check`, 응답에 `nosniff` · `DENY` · `no-referrer` 헤더. *터미널형*(Terminal–1): `.cmd` → `.ps1` 진입(`-ExecutionPolicy Bypass`, 그룹 정책은 우회하지 않음), 모든 칸에 `?` 가이드, 폭 1칸 아이콘만, `--once` 로 한 장 찍기 |
| S-05 | **Windows 가 본 무대.** 경로는 항상 큰따옴표로 감싸고, 명령은 cmd · PowerShell · Git Bash 에서 그대로 돌게 쓴다. PowerShell 5.1 호환. `.ps1` · `.cmd` · `.bat` 은 CRLF (`.ps1` 은 UTF-8 BOM). 콘솔 출력은 UTF-8 |
| S-06 | **대장 먼저.** 새 앱은 코드보다 먼저 [대장](docs/REGISTRY.md)에 이름 · 폴더 · 포트 대역 · 전역 단축키 · 데이터 폴더 · 환경 변수 · 캐릭터를 등록한다 |
| S-07 | **문서.** [뼈대](docs/templates/)에서 시작한다. 앱 README 는 루트 README 만 링크하고(`explore ›`), 형제 앱으로 직접 링크하지 않는다. 시스템 공용 이미지(system · parts · palette · colophon)는 루트 `docs/page/` 에만 둔다. 앱 문서 이미지는 앱당 3 MB 까지 |
| S-08 | **커밋.** 한국어로, `<name>-<n>: 무엇 · 무엇` (저장소 전체는 `works: …`). 한 커밋에서 앱 두 개를 고치지 않는다 |

---

## 새 앱 만드는 순서

1. [대장](docs/REGISTRY.md)에 등록한다 — 코드 없는 PR 하나
2. [docs/templates/](docs/templates/) 를 복사해 폴더 뼈대를 만든다
3. 구현 · 테스트 · 워크플로
4. `python tools/works_check.py` 에 새 경고가 없게 한다
5. 루트 README 의 폴더 표와 공용 이미지(`docs/page/`)를 고친다. 다른 앱 README 는 건드리지 않는다

## 검사기

```text
python tools/works_check.py            모든 앱 검사 (위반이 있어도 종료 코드 0 — 고치는 중에 볼 때)
python tools/works_check.py --strict   위반이 있으면 종료 코드 1 (CI 가 쓰는 차단 모드)
python tools/works_check.py --no-tests 테스트 수 확인(테스트 불러오기)을 건너뜀
```

- 모든 push · PR 에서 `.github/workflows/works.yml` 이 `--strict` 로 돌린다. **예외 대장에 없는 위반이 하나라도 있으면 CI 가 실패한다.**
- 예외 대장의 기한이 지나면 그 위반도 다시 세므로, 기한 날 CI 가 실패한다. 고치거나, 이유를 적고 기한을 늦추는 PR 을 낸다.
- 검사기가 못 보는 조항(W-05 · W-07 · W-09 등)은 리뷰에서 본다.

## 규칙 바꾸기

- 헌법은 12조를 넘기지 않는다. 새 조항은 실제로 일어난 문제나 두 앱 이상에서 되풀이된 패턴이 근거일 때만 넣는다.
- 조항을 바꾸는 PR 은 그 조항만 바꾼다. 검사기 · AGENTS.md 요약 · 예외 대장도 같은 PR 에서 맞춘다.
